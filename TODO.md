# TODO

## Android’de npm ile kontrol + sorun yoksa adb ile telefona yükleme

- [x] `11/scripts/deploy-android.sh` oluştur: build + apk doğrulama + cihaz kontrol + `adb install -r` yükleme
- [x] `11/package.json` içine `deploy:android` ve `deploy:android:prod` script’lerini ekle
- [x] Çalıştırma komutlarını doğrula: `npm run deploy:android` / `npm run deploy:android:prod`
- [x] (Opsiyonel) Uygulamayı otomatik açma adımı ekle ve paketi doğrula



